using UnityEngine;

public class test_asd : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("테스트용");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
